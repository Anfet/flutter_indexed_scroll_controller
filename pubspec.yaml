name: indexed_scroll_controller
description: "scroll to a targeted index in flutter list view"
version: 0.0.1
homepage: https://github.com/Anfet/flutter_indexed_scroll_controller

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
